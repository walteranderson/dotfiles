- defaults:
    link:
        relink: true

- clean: ['~']

- link:
    ~/.vimrc:
    ~/.vim:
    ~/.zshrc:
    ~/.zsh:
    ~/.shell:
    ~/.ideavimrc:
    ~/.bashrc:
    ~/.gvimrc:
    ~/.gitconfig:
    ~/.tmux.conf:
    ~/.config/tmuxinator: tmuxinator
    ~/.bash_profile:
    ~/.emacs.d:

- shell:
  - git submodule update --init --recursive
  - ./homebrew/install
  - ./shell/install-yarn
