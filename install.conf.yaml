- defaults:
    link:
        relink: true

- clean: ['~']

- link:
    ~/.vim:
    ~/.zshrc:
    ~/.zsh:
    ~/.shell:
    ~/.vimrc:
    ~/.bashrc:
    ~/.gvimrc:
    ~/.eslintrc:
    ~/.gitconfig:
    ~/.tmux.conf:
    ~/.tmuxinator:
    ~/.bash_profile:
    ~/.hammerspoon:
