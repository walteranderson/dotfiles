- defaults:
    link:
        relink: true

- clean: ['~']

- link:
    ~/.vimrc:
    ~/.vim:
    ~/.zshrc:
    ~/.zsh:
    ~/.shell:
    ~/.ideavimrc:
    ~/.bashrc:
    ~/.gvimrc:
    ~/.gitconfig:
    ~/.tmux.conf:
    ~/.config/tmuxinator: tmuxinator
    ~/.bash_profile:

- shell:
  - git submodule update --init --recursive
  - ./homebrew/install
  - ./shell/install-yarn
